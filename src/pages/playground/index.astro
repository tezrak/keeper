---
import { getTheme } from "../../domains/utils/getTheme";
import Layout from "../../layouts/Layout.astro";
import { Page } from "./_page";

const theme = getTheme({});
---

<Layout
  title={[`Playground`]}
  description={undefined}
  theme={theme}
  withDefaultBanner
>
  <Page client:only="react" theme={theme} />
</Layout>
