---
import { getTheme } from "../../domains/utils/getTheme";
import Layout from "../../layouts/Layout.astro";
import { CampaignsPage } from "./_index";

const theme = getTheme({});
---

<Layout title="Campaigns" withBanner h1="Campaigns">
  <CampaignsPage client:load theme={theme} />
</Layout>
